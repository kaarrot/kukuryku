/target
/vendor

# tract conv bench outputs (machine-specific)
tools/bench_out/

# project-local Kokoro model + split subgraphs (split_kokoro.py default output).
# Kept out of the repo: the fp32 subgraphs are ~311 MB and don't compress.
kokoro-onyx/

# nested tract crate build artifacts
third_party/tract/**/target/
